function charCase(alph) {
    if (ch>='A' && ch<='Z') {
        ch1= character.toLowerCase(ch);
        return " lowerCase" + ch1;}
    else 
        ch1= character.upperCase(ch);
        return " upperCase" + ch1;
    }
console.log(charCase(A));
