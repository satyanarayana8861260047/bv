var msg1 = {to:8073068972,
            msg:"How are you ?",
            delivered:true,
            read:false,
            from:8861260047};

for(prop in msg1)
    console.log(prop);

    for(property in msg1);
        console.log(property + ":" + msg1[property]);

    console.log('--------');
    console.log(msg1["delivered"]);
    var keyX = "delivered";
    console.log([msg1[keyX]]);
